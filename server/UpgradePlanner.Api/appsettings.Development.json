{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "//": "Locally the generated SQL is worth seeing. EF masks parameter values as '?', so this still never prints user content - but it is loud, which is why production keeps it at Warning.",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  }
}
